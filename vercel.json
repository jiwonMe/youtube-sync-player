{
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next",
      "config": {
        "rootDir": "apps/web"
      }
    }
  ],
  "buildCommand": "pnpm --filter=web run build",
  "installCommand": "pnpm install"
}
